This project contains some utilities that can be useful when you have to administer MySQL servers.

-dbCopy.sh:
    This tools can replicate databases in different instances or servers MySQL.
    It's quite useful for really big databases, due to it doesn't use any temporary disk space (it use a pipe to move the data). It use a CSV export and import mecanism instead of standard MySQL backup utility, and this increase significantly the performance.